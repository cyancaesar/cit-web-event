'use client';

import { Button } from '@/components/ui/button';
import { generateDocument } from '@/utils/generateDocument';
import React from 'react';

export default function GenerateDoc() {
  const handleClick = () => {};

  return <Button onClick={() => handleClick()}>جرب</Button>;
}
